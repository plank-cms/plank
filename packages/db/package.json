{
  "name": "@plank-cms/db",
  "version": "0.27.4",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && mkdir -p dist/migrations && cp src/migrations/*.sql dist/migrations/",
    "dev": "mkdir -p dist/migrations && cp src/migrations/*.sql dist/migrations/ && tsc --watch",
    "lint": "eslint src/"
  },
  "dependencies": {
    "pg": "8.20.0"
  },
  "devDependencies": {
    "@types/pg": "8.20.0"
  }
}
